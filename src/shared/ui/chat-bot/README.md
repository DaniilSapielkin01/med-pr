# ChatBot Component

Красивый и профессиональный чат-бот компонент с анимациями для интеграции с RAG системой.

## Использование

### Базовое использование

Компонент автоматически использует `/api/chat` endpoint:

```tsx
import { ChatBot } from "@/shared/ui";

export default function Page() {
  return <ChatBot />;
}
```

### С кастомным endpoint

```tsx
import { ChatBot } from "@/shared/ui";

export default function Page() {
  return (
    <ChatBot
      apiEndpoint="/api/chat"
      initialMessage="Привіт! Я ваш помічник. Чим можу допомогти?"
      placeholder="Напишіть ваше питання..."
    />
  );
}
```

### С кастомной функцией обработки сообщений

```tsx
import { ChatBot } from "@/shared/ui";

export default function Page() {
  const handleMessage = async (message: string): Promise<string> => {
    const response = await fetch("/api/chat", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ message }),
    });
    const data = await response.json();
    return data.answer;
  };

  return <ChatBot onMessageSend={handleMessage} />;
}
```

## Props

- `apiEndpoint?: string` - URL endpoint для отправки сообщений
- `initialMessage?: string` - Начальное сообщение от бота (по умолчанию: "Привіт! Я ваш помічник. Чим можу допомогти?")
- `placeholder?: string` - Placeholder для поля ввода (по умолчанию: "Напишіть ваше питання...")
- `onMessageSend?: (message: string) => Promise<string>` - Кастомная функция для обработки сообщений

## Особенности

- ✨ Плавные анимации с framer-motion
- 🎨 Современный дизайн с использованием цветов проекта (#43ae43)
- 📱 Адаптивный дизайн для мобильных устройств
- 🔄 Автоматическая прокрутка к новым сообщениям
- ⚡ Автоматическое изменение размера поля ввода
- 💬 Поддержка истории сообщений
- 🎯 Минимизация окна чата
- ⌨️ Отправка сообщения по Enter (Shift+Enter для новой строки)

## API Endpoint Format

Компонент использует существующий API endpoint `/api/chat` (файл `src/api/chat/chat.ts`).

### Формат запроса:

```json
{
  "message": "текст сообщения пользователя"
}
```

### Формат ответа:

```json
{
  "answer": "ответ бота"
}
```

API endpoint автоматически:

- Читает `sitemap-rag.json` для контекста
- Использует OpenAI API через OpenRouter
- Возвращает короткие ответы (до 250 символов) українською мовою
- Додає посилання на відповідні сторінки сайту
